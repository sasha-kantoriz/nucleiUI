services:
  nucleiui:
    build: .
    restart: always
    ports:
      - 5000:5000
    environment:
      - SCRIPT_NAME=/nucleiui
    logging:
      driver: "json-file"
      options:
        max-size: "10m"